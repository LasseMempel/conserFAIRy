# Active Context

## Current Focus


## Next Steps
- Implement the `<Concept>Storage` and `<Concept>Quality` dual-model generation logic.
- Set up the basic Django-ninja API structure.
- Set up the SKOS TTL -> Pydantic -> JSON Schema generation pipeline in `schemas/scripts/`.




/home/mempellaenger/repos/conserFAIRy/backend