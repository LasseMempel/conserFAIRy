name: fastapi-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - uvicorn
  - aiosqlite
  - pip:
    - fastapi
    - fastapi-users[sqlalchemy]
    - "uvicorn[standard]"